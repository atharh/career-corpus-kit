---
jd: jd.md
checked: <YYYY-MM-DD>
corpus_pin: <the corpus commit this check read — an input, and unrecoverable later>
---

# Fit — <Company>, <Role>

## Requirements

<The posting's real requirements, in its own words, one per row. Evidence state is exactly one
of `backed`, `thin`, `no-corpus-evidence` — see the apply skill's fit-check section. The last
column is the user's and starts empty.>

| Requirement | Evidence state | Corpus evidence | Their call |
|---|---|---|---|
| <requirement, quoted> | backed | `corpus/<company>/<story>.md` | |
| <requirement, quoted> | thin | `corpus/<company>/<story>.md` — <what folds under a second question> | |
| <requirement, quoted> | no-corpus-evidence | none | |

## Discarded as boilerplate

<Which requirements were judged not real, and why — so the user can disagree.>

## What these judgements rest on

<Name any row that was computed from a rendered artifact rather than from a story file. Those
inherit the artifact's errors, and a gap that is really a stale bullet reads exactly like a gap
in the corpus.>

## The read

<One short paragraph, ending in a recommendation the user can overrule. "Don't apply yet" is a
valid result. Where the corpus is thin, give a pointer rather than a verdict: the closest story
file, and what an interview session would have to produce.>
