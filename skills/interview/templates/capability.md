---
title: <technology> — what they can actually be asked about
kind: capability file — one technology, whole career. See `[CAPABILITY-FILE]`.
status: <opened YYYY-MM-DD | interviewed, rounds so far | renderable within its ceilings>
answers_the_question: |
  <The question this file exists to answer, as it would actually be asked — out loud in a
  loop, or as a self-rating box on a form. If a real application prompted it, say which.>
owns_no_facts: |
  This file is a derived index (`[OWNS-NO-FACTS]`). Every fact cites the story file that owns
  it. A fact recorded here with no home yet is marked homeless and pushed down into its owning
  company or project file once placement settles.
sources:
  - interview with <user>, YYYY-MM-DD
---

## The answer, in the shape they'd say it

<Two or three spoken sentences, honest to the ceilings below. This is what the file renders
to when the question arrives; everything after it is the evidence and the limits.>

## Beat: <what they did with it — the strongest claim first>

<What they actually did, where, citing the owning story file — `../<company>/<story>.md`.
Each claim states its depth ceiling: how far it can be pushed before it breaks.
*Administered it* and *my team ran on it* are different claims (`[DEPTH-CEILING]`); never
let them read alike.>

## The noes — what makes the rest credible

<What they have NOT done with this technology, plainly. The analogue of a through-line's
"where it doesn't hold". An interviewer trusts the yeses in proportion to these.>

## Contradictions held — not resolved here

<When an answer collides with a ceiling another file records, block it here and wait for the
owner of that ceiling to settle it (`[CROSS-FILE-CEILINGS]`, `[MARK-DONT-FIX]`). Nothing
below this heading renders.>

## Gaps — the interview queue

<Candidate claims still to disprove — the user's own listed uses of this technology, each
awaiting "what actually ran on it" (`[LIST-IS-A-QUEUE]`). Plus anything that passes the
say-it-out-loud test `[SAY-ALOUD]`.>

- [ ] **<technology they named, not yet asked what ran on it>**
